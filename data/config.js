/**
 * Store Configuration
 * ─────────────────────────────────────────────
 * Update your WhatsApp number here.
 * Format: country code + number, NO +, NO spaces, NO dashes.
 *
 * Example:
 *   India (+91) 98765 43210  →  919876543210
 */

const storeConfig = {
  // 📱 WhatsApp number for receiving orders (international format, no + or spaces)
  whatsappNumber: '919039125961',

  // 🏪 Store name shown in the WhatsApp message
  storeName: 'TheEyeConic',
};

export default storeConfig;
